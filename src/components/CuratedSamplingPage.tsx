import { SamplingExperience } from '../sampling/app/SamplingExperience';

/** Isolated curated sampling prototype — not linked from main site navigation. */
export const CuratedSamplingPage = () => <SamplingExperience />;
